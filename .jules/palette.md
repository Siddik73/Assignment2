## 2024-10-24 - Interactive Div to Semantic Button
**Learning:** The brick mini-game used a `<div>` with a click handler, preventing keyboard users from interacting with it.
**Action:** Always use semantic `<button>` elements for interactive components that trigger actions to ensure out-of-the-box keyboard accessibility (Tab focus and Space/Enter activation).
