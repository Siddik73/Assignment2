## 2024-08-29 - Semantic buttons for interactive elements
**Learning:** Using `div` for click targets like the minigame brick breaks keyboard accessibility and screen reader support.
**Action:** Always use semantic `<button>` elements with `aria-label` for interactive non-text UI elements to ensure a11y.
